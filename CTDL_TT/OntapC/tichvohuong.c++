#include<iostream>
using namespace std ; 
int main () {
    int n ; 
    cin >> n ; 

    int arrx[n] ; 
    int arry[n] ; 

    for ( int i = 0 ; i < n ; i++ ){
        cin >> arrx[i];
    }
    for ( int i = 0 ; i < n ; i++ ) {
        cin >> arr
    }
}